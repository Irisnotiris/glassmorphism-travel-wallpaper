---
name: glassmorphism-travel-wallpaper
description: 将任意上传图片设计为高级玻璃拟态旅行信息卡手机锁屏壁纸（Glassmorphism / 毛玻璃 / Frosted Glass Travel Wallpaper）。9:19.5 超竖版（1080×2340）；上传图作为全屏背景（轻微虚化、低饱和、柔和雾感），玻璃卡片居中偏下、顶部留出空间给锁屏时间，质感参考 Apple Vision Pro / iOS Glass UI。当用户上传任意照片/图片，要求做成毛玻璃旅行信息卡手机壁纸、玻璃拟态锁屏壁纸、frosed glass travel wallpaper、Apple 风格玻璃卡片手机壁纸，或提到"手机壁纸 / 锁屏壁纸 / 9:19.5 / 玻璃拟态壁纸"时使用。
---

# Glassmorphism Travel Wallpaper

把用户上传的任意一张图片，设计为一张 9:19.5 超竖版「高级玻璃拟态旅行信息卡手机锁屏壁纸」。

与 `glassmorphism-travel-card`（3:4 海报版）的区别：本 skill 专为手机锁屏优化——画幅 9:19.5、卡片居中偏下、顶部留白给时间。

## 输入与准备

1. **读取上传图片**：下载/读取用户上传的图片，识别画面主体、场景与主色调，用于填入 `{{IMAGE_DESCRIPTION}}` 和判断预览窗裁切区域。不要改动原图文件。
2. **取得参考图 URL**：本地路径先调用 `FileBatchUpload` 上传获取 URL；已是 URL 直接用。
3. **读取主模板**：打开 `references/prompt-template.md`，逐项保留其质感与布局要求，不得删减。

## 组装 prompt

- 用一句话把原图内容填入 `{{IMAGE_DESCRIPTION}}`。
- **地点/天气文案动态推断**：根据上传图片的场景、地点、氛围，自动推断匹配的英文文案填入 `{{DISTANCE}}`、`{{PLACE_TITLE}}`、`{{SUBTITLE}}`、`{{WEATHER_LINE1}}`、`{{WEATHER_LINE2}}`，不使用固定值。例如图片是雪山则推断山地地点与寒冷天气，图片是海滩则推断海滨地点与海风天气。用户明确指定了文案时，以用户指定为准。全部使用英文。
- 画内文字一律用中文引号「“ ”」标注。

## 生成

- 调用 `image_edit`（参考生成），`image_reference_url_list=[原图 URL]`，`model_version=seedream_4.5`（用户明确要求 5.0 Pro 时用 `seedream_5.0_pro`）。
- 画幅固定 9:19.5 超竖版：`height=2340, width=1080`。
- `prompt` = 主模板全文（含替换后的占位符）。

## 质量自检

用 `Read` 查看生成图（thumbnail_size 至少 medium），逐项检查：

1. **构图**：玻璃卡片小巧紧凑、高度较短、位于画面下半部分位置靠下，上半部分大面积干净留白给锁屏时间，整体呼吸感舒适；
2. 背景保留原图主体，轻微虚化低饱和雾感；
3. 玻璃卡片是「真磨砂玻璃」：极细白色高光描边、边缘发亮、内部折射/模糊、极淡光斑——不能像灰色遮罩或塑料；
4. 所有元素齐全：搜索框（“Search place...”+“+”）、预览图、距离文字、地点标题副标题、“Directions”、两行天气；且文案与图片场景匹配、全英文；
5. **每段文字只出现一次，无重复，无多余文字（不含状态栏/时间/电池等）**。

任一不合格：强化对应缺陷措辞重新生成，最多重试 2 次；仍不达标则保留最佳一张并如实说明。

## 交付

- 用 `present_files` 交付最终壁纸。
- 交付说明写明画幅（9:19.5 / 1080×2340）、适用场景（锁屏壁纸）与检查结果。
