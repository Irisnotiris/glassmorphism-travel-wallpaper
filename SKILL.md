---
name: glassmorphism-travel-wallpaper
description: 将任意上传图片设计为高级玻璃拟态旅行信息卡手机锁屏壁纸（Glassmorphism / 毛玻璃 / Frosted Glass Travel Wallpaper）。9:19.5 超竖版（1080×2340）；通过图片分析+联网查询推测真实地点，独立生成英文文案，再生成毛玻璃卡片壁纸。当用户上传任意旅行照片/风景图片，要求做成毛玻璃旅行信息卡手机壁纸、玻璃拟态锁屏壁纸、frosed glass travel wallpaper，或提到"手机壁纸 / 锁屏壁纸 / 9:19.5 / 玻璃拟态壁纸"时使用。
---

# Glassmorphism Travel Wallpaper

把用户上传的任意一张图片，通过多阶段工作流设计为一张 9:19.5 超竖版「高级玻璃拟态旅行信息卡手机锁屏壁纸」。

**核心区别于普通提示词 skill**：地点和天气文案不是让生图模型瞎编，而是先分析图片、联网推测真实地点、再独立生成文案，最后才填入提示词生成。

## 工作流总览

```
阶段1 图片分析 → 阶段2 联网推测地点 → 阶段3 文案生成 → 阶段4 生成壁纸 → 阶段5 质检交付
```

每个阶段有明确的输入输出，不得跳过。用户明确指定了地点/文案时，跳过阶段2-3，直接用用户给的文案。

---

## 阶段1：图片分析

**目标**：理解图片内容，提取用于地点推测和文案生成的结构化线索。

1. 读取上传图片（本地路径直接 Read，URL 先下载）。
2. 仔细观察并输出以下分析结果（用文字记录，不需要生成文件）：
   - **场景类型**：海滩 / 山地 / 城市 / 森林 / 沙漠 / 湖泊 / 古镇 / 其他
   - **地理特征**：有无海洋、山脉、悬崖、岛屿、特定地貌
   - **建筑风格**：地中海白墙彩屋 / 欧式古堡 / 东亚传统 / 现代都市 / 木屋 / 其他
   - **标志性元素**：圆顶教堂、塔楼、桥梁、特定植被（棕榈/松树/仙人掌）
   - **主色调与氛围**：冷暖、明暗、安静/热闹
   - **主体位置**：画面主体在哪个区域，用于预览窗裁切
3. 用一句话总结 `{{IMAGE_DESCRIPTION}}`，用于后续填入生图 prompt。

**参考**：详细的观察维度见 `references/image-analysis.md`。

---

## 阶段2：联网推测真实地点

**目标**：根据图片分析结果，找到最相似的真实地点，让文案有事实依据。

1. 从阶段1的分析中提取 2-3 个搜索关键词组合，例如：
   - "地中海 悬崖 彩色小镇 圆顶教堂"
   - "瑞士 雪山 湖泊 木屋"
   - "日本 竹林 寺庙 石阶"
2. 调用 `general_search` 并行搜索（最多 3 个 query），查找最匹配的真实地名。
3. 综合搜索结果，确定**最可能的地点**：
   - 地点名（英文，如 "Positano"）
   - 地区/国家（英文，如 "Amalfi Coast, Italy"）
   - 置信度：高 / 中 / 低
4. 如果搜索结果不明确或置信度低，使用一个合理的泛化描述（如 "Coastal Village" / "Mountain Retreat"），不要硬编一个具体地名。

**参考**：搜索策略和关键词构造见 `references/location-search.md`。

---

## 阶段3：文案生成

**目标**：基于确定的地点，独立生成全部英文文案，不再让生图模型推断。

根据阶段2的地点结果，生成以下文案（全部英文）：

| 占位符 | 说明 | 示例 |
|---|---|---|
| `{{PLACE_TITLE}}` | 地点标题 | "Positano" |
| `{{SUBTITLE}}` | 地区/国家 | "Amalfi Coast, Italy" |
| `{{DISTANCE}}` | 距离标签 | "1.2 km" / "850m" |
| `{{WEATHER_LINE1}}` | 第一行天气 | "Sea breeze · 12 km/h" |
| `{{WEATHER_LINE2}}` | 第二行天气 | "Golden Hour in 42 min" |

**生成规则**：
- 距离：根据场景合理推测（海边小镇用几百米到1-2km，山地用海拔或距离）
- 天气：根据地点的典型气候和图片氛围生成（海滩→海风/晴，雪山→微风/低温，森林→晨雾/湿度）
- 第二行优先用时间/光线相关描述（Golden Hour / Sunset / Blue Hour），增加氛围感
- 生成 2 组候选文案，自动选最贴合图片氛围的一组；用户在场时可简短展示供选择

**参考**：文案风格和模板见 `references/copywriting.md`。

---

## 阶段4：生成壁纸

1. **裁剪图片**：如果上传图片比例不是 9:19.5，用 `scripts/crop_to_ratio.py` 中心裁剪：
   ```
   python scripts/crop_to_ratio.py <原图路径> <裁剪后路径> --ratio 9:19.5
   ```
2. **获取参考图 URL**：裁剪后的图片用 `FileBatchUpload` 上传获取 URL。
3. **读取主模板**：打开 `references/prompt-template.md`。
4. **填入占位符**：
   - `{{IMAGE_DESCRIPTION}}` ← 阶段1的一句话描述
   - `{{PLACE_TITLE}}` / `{{SUBTITLE}}` / `{{DISTANCE}}` / `{{WEATHER_LINE1}}` / `{{WEATHER_LINE2}}` ← 阶段3的文案
   - 画内文字一律用中文引号「“ ”」标注
5. **调用 `image_edit`**：
   - `image_reference_url_list=[裁剪后图片 URL]`
   - `model_version=seedream_4.5`（用户要求 5.0 Pro 时用 `seedream_5.0_pro`）
   - `height=2340, width=1080`
   - `prompt` = 填入占位符后的完整模板

---

## 阶段5：质检与交付

用 `Read` 查看生成图（thumbnail_size 至少 medium），逐项检查：

1. **构图**：卡片小巧紧凑、位于下半部分，上半部分大面积留白给锁屏时间；
2. **背景**：是原图的虚化版本，保留原图构图和主体，未被重新生成；
3. **玻璃质感**：极细白色高光描边、边缘发亮、内部折射/模糊、极淡光斑——不像灰色遮罩或塑料；
4. **元素齐全**：搜索框（“Search place...”+“+”）、预览图、距离文字、地点标题副标题、“Directions”、两行天气；
5. **文案准确**：地点名和天气与阶段2-3确定的一致，无乱码；
6. **文字唯一**：每段文字只出现一次，无重复，无多余元素（状态栏/时间/电池等）。

任一不合格：强化对应缺陷措辞重新生成，最多重试 2 次；仍不达标则保留最佳一张并如实说明。

**交付**：用 `present_files` 交付最终壁纸。交付说明写明画幅（9:19.5 / 1080×2340）、推测的地点、使用的文案、检查结果。

---

## 文件结构

```
glassmorphism-travel-wallpaper/
├── SKILL.md                          # 本文件：五阶段工作流
├── references/
│   ├── prompt-template.md            # 生图提示词模板
│   ├── image-analysis.md             # 图片分析指南
│   ├── location-search.md            # 联网推测地点策略
│   └── copywriting.md                # 文案生成指南
└── scripts/
    └── crop_to_ratio.py              # 图片中心裁剪工具
```
